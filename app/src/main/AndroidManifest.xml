<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gimnasio">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.ConfirmUserActivity"></activity>
        <activity android:name=".Activity.InfoUsuarioActivity" />
        <activity android:name=".Activity.AgregarUsuarioActivity" />
        <activity android:name=".Fragment.PerfilAdminFragment" />
        <activity android:name=".Activity.RegistroActivity" />
        <activity android:name=".Activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.MainActivity" />
        <activity android:name=".Activity.InfoTurnoActivity" />
        <activity android:name=".Activity.UsuarioVerificarActivity" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>